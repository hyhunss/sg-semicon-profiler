#:schema https://developers.openai.com/codex/config-schema.json

# Project-level configuration for zero-setup multi-agent discovery.
[features]
multi_agent = true

[agents]
enabled = true
max_concurrent_threads_per_session = 6
max_depth = 1
